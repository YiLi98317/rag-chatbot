# 本地启动指南 — 从零到 Chatbot 可用

## 前置条件

- macOS，Python 3.10+
- Ollama 已安装并运行（`ollama serve`）
- 已有模型：`qwen3-embedding`（embedding）、`qwen3:8b`（chat）

如果还没拉 chat 模型：
```bash
ollama pull qwen3:8b
```

---

## 第一步：安装项目依赖

```bash
cd rag-chatbot
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

如果不想用 venv，直接 `pip3 install -r requirements.txt` 也行，但推荐 venv。

---

## 第二步：确认 .env 配置

当前 `.env` 已配置为全本地模式（ollama embedding + ollama chat + milvus lite）：

```
OLLAMA_BASE_URL=http://localhost:11434
EMBED_PROVIDER=ollama
EMBED_MODEL=qwen3-embedding
CHAT_MODEL=qwen3:8b
LLM_PROVIDER=ollama
VECTOR_PROVIDER=milvus
MILVUS_LITE_DB=./milvus.db
MILVUS_COLLECTION=chatbot_docs
ANSWER_PERSONA=phone_mom
```

不需要任何 API key，不需要 Docker，不需要外网。

---

## 第三步：导入知识库

知识库 markdown 已生成在 `data/target/知识库.md`。

```bash
# 导入知识库（全量重建）
DATA_DIR=data/target make ingest collection=chatbot_docs args="--recreate"
```

这会把 `data/target/` 下的所有 `.md`/`.txt`/`.xlsx` 文件分块、embedding、写入 Milvus。

---

## 第四步：导入七鱼对话数据（可选，增强检索）

```bash
# 先用少量测试（100条，至少2轮对话）
python reingest_qiyu.py \
  --xlsx data/shangwu11to03.xlsx \
  --collection chatbot_docs \
  --mode both \
  --max-sessions 100 \
  --min-rounds 2

# 效果满意后全量导入
python reingest_qiyu.py \
  --xlsx data/shangwu11to03.xlsx \
  --collection chatbot_docs \
  --mode both
```

注意：不要加 `--recreate`，否则会覆盖第三步导入的知识库。

---

## 第五步：测试 Chatbot

```bash
# 交互式聊天
make chat collection=chatbot_docs

# 单次查询
make query q="怎么锁机" collection=chatbot_docs

# 调试模式（看检索到了哪些内容）
make chat collection=chatbot_docs DEBUG=1
```

---

## 第六步：启动 HTTP API（可选）

```bash
make api
# 然后访问 http://localhost:8000/healthz
# POST http://localhost:8000/v1/qa  body: {"question": "怎么锁机"}
```

---

## 数据迭代流程

```
1. 修改/补充 data/target/ 下的知识库文件
2. 重新 ingest：DATA_DIR=data/target make ingest collection=chatbot_docs args="--recreate"
3. 测试：make chat collection=chatbot_docs
4. 不满意 → 回到 1
```

---

## 常见问题

**Q: ingest 很慢？**
A: ollama embedding 比 sentence_transformers 慢。如果想快一点，可以把 `.env` 改回 `EMBED_PROVIDER=sentence_transformers` + `EMBED_MODEL=BAAI/bge-m3`（首次需下载模型，约 2GB）。

**Q: chat 回答质量不好？**
A: 先用 `DEBUG=1` 看检索到的内容是否相关。如果检索到的内容不对，说明知识库需要补充。如果检索到了但回答不好，可以换更大的模型（如 `qwen3:14b`）。

**Q: 想切回 DeepSeek API？**
A: `.env` 里改 `LLM_PROVIDER=deepseek`，填上 `API_KEY`。embedding 可以继续用 ollama。
