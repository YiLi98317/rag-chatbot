apiVersion: v1
kind: Secret
metadata:
  name: chatbot-api-secrets
type: Opaque
stringData:
  # If you switch VECTOR_PROVIDER=qdrant, set these:
  # QDRANT_API_KEY: ""
  #
  # If your Ollama requires auth/proxy credentials, set them here (placeholders).
  DUMMY: "replace-me"

