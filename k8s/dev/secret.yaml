apiVersion: v1
kind: Secret
metadata:
  name: rag-api-secrets
type: Opaque
stringData:
  # Placeholder to keep envFrom secretRef stable.
  # If you switch VECTOR_PROVIDER=qdrant, set QDRANT_API_KEY here.
  DUMMY: "replace-me"

