# RAG 智能客服机器人 — 部署与使用指南

## 系统概览

| 组件           | 技术选型                                   |
| -------------- | ------------------------------------------ |
| Embedding 模型 | Qwen3-Embedding-8B (4096维, fp16, GPU推理) |
| LLM 回答       | DeepSeek API (deepseek-chat)               |
| 向量数据库     | Milvus Lite (本地文件 milvus.db)           |
| API 框架       | FastAPI + Uvicorn                          |
| 服务器         | 阿里云 ECS, NVIDIA A10 24GB, 58GB RAM      |

## 目录结构

```
/root/ragchatbot/
├── .env                        # 环境配置
├── milvus.db                   # 向量数据库（ingest后生成）
├── api/
│   ├── app.py                  # FastAPI 应用
│   └── models.py               # 请求/响应模型
├── src/chatbot/
│   ├── embeddings/
│   │   ├── st_embedder.py      # Sentence-Transformers embedding (GPU)
│   │   └── provider.py         # Embedding 统一入口
│   ├── ingest/
│   │   ├── loader.py           # 文件加载（md/txt/xlsx）
│   │   ├── chunking.py         # 中英文分块
│   │   └── qiyu_parser.py      # 七鱼对话解析
│   ├── service/
│   │   └── qa_service.py       # QA 服务（检索+生成）
│   └── settings.py             # 配置中心
├── reingest_qiyu.py            # 七鱼Excel对话导入脚本
├── data/
│   ├── shangwu11to03.xlsx      # 对话数据（31K+会话）
│   └── target/
│       └── 知识库.md            # 知识库文档
└── ingest_full.log             # 最近一次导入日志
```

---

## 一、首次部署

### 1. 环境准备

```bash
# 安装 Miniconda（如未安装）
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh

# 创建 conda 环境
conda create -n rag python=3.12 -y
conda activate rag

# 安装 PyTorch（匹配 GPU 驱动的 CUDA 版本）
# 先运行 nvidia-smi 查看 CUDA Version，选择对应版本：
# CUDA 12.x:
pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# 如果 sympy 等依赖超时，先用阿里云镜像安装：
pip install sympy==1.13.1 pillow -i https://mirrors.aliyun.com/pypi/simple/ --trusted-host mirrors.aliyun.com

# 安装项目依赖
cd /root/ragchatbot
pip install -r requirements.txt
```

### 2. 验证 GPU

```bash
python -c "import torch; print('CUDA:', torch.cuda.is_available(), torch.cuda.get_device_name(0))"
# 预期输出: CUDA: True NVIDIA A10
```

### 3. 下载 Embedding 模型

```bash
# 通过 HuggingFace 国内镜像下载 Qwen3-Embedding-8B（约15GB）
source /root/miniconda3/bin/activate rag
HF_ENDPOINT=https://hf-mirror.com python -c "
from huggingface_hub import snapshot_download
snapshot_download('Qwen/Qwen3-Embedding-8B')
print('Done')
"
```

### 4. 配置 .env

```bash
cat > /root/ragchatbot/.env << 'EOF'
VECTOR_PROVIDER=milvus
MILVUS_LITE_DB=./milvus.db
MILVUS_DB=default
MILVUS_COLLECTION=chatbot_docs

EMBED_PROVIDER=sentence_transformers
EMBED_MODEL=Qwen/Qwen3-Embedding-8B

LLM_PROVIDER=deepseek
API_KEY=<你的DeepSeek API Key>
API_BASE_URL=https://api.deepseek.com
MODEL_NAME=deepseek-chat

OLLAMA_BASE_URL=http://localhost:11434
CHAT_MODEL=qwen3:8b

TOP_K=5
ZH_CHUNK_SIZE=1200
ZH_CHUNK_OVERLAP=150

ANSWER_PERSONA=phone_mom

HF_ENDPOINT=https://hf-mirror.com
EOF
```

---

## 二、数据导入 (Ingest)

### 导入对话数据（七鱼 Excel）

```bash
source /root/miniconda3/bin/activate rag && cd /root/ragchatbot

# 删除旧数据库重新导入
rm -f milvus.db

nohup bash -c '
export HF_ENDPOINT=https://hf-mirror.com
export PYTHONPATH=/root/ragchatbot/src
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

python reingest_qiyu.py \
  --xlsx data/shangwu11to03.xlsx \
  --recreate --mode both \
  --embed-batch-size 8 \
  --upsert-batch-size 64 \
  --min-rounds 2
' > /root/ragchatbot/ingest_qiyu.log 2>&1 &

echo "Monitor: tail -f /root/ragchatbot/ingest_qiyu.log"
```

参数说明：

- `--mode both`: 同时生成 knowledge（知识提取）和 conversation（对话上下文）两种 chunk
- `--embed-batch-size 8`: GPU 显存安全值（A10 24GB，模型占16GB）
- `--min-rounds 2`: 跳过对话轮数不足2轮的无效会话

### 导入知识库文档

在对话数据导入完成后，追加知识库（不要加 `--recreate`）：

```bash
source /root/miniconda3/bin/activate rag && cd /root/ragchatbot

nohup bash -c '
export HF_ENDPOINT=https://hf-mirror.com
export PYTHONPATH=/root/ragchatbot/src
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

DATA_DIR=/root/ragchatbot/data/target python -m chatbot.cli.ingest \
  --chunk-size 1200 \
  --chunk-overlap 150 \
  --embed-batch-size 8 \
  --batch-size 64
' > /root/ragchatbot/ingest_kb.log 2>&1 &

echo "Monitor: tail -f /root/ragchatbot/ingest_kb.log"
```

### 一键全量导入（对话+知识库）

```bash
source /root/miniconda3/bin/activate rag && cd /root/ragchatbot && rm -f milvus.db && nohup bash -c '
export HF_ENDPOINT=https://hf-mirror.com
export PYTHONPATH=/root/ragchatbot/src
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

python reingest_qiyu.py \
  --xlsx data/shangwu11to03.xlsx \
  --recreate --mode both \
  --embed-batch-size 8 \
  --upsert-batch-size 64 \
  --min-rounds 2

DATA_DIR=/root/ragchatbot/data/target python -m chatbot.cli.ingest \
  --chunk-size 1200 \
  --chunk-overlap 150 \
  --embed-batch-size 8 \
  --batch-size 64
' > /root/ragchatbot/ingest_full.log 2>&1 &

echo "Monitor: tail -f /root/ragchatbot/ingest_full.log"
```

---

## 三、启动 API 服务

```bash
source /root/miniconda3/bin/activate rag && cd /root/ragchatbot

nohup bash -c '
export HF_ENDPOINT=https://hf-mirror.com
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
PYTHONPATH=/root/ragchatbot/src uvicorn api.app:app --host 0.0.0.0 --port 8000
' > /root/ragchatbot/api.log 2>&1 &

echo "API started on port 8000"
echo "Monitor: tail -f /root/ragchatbot/api.log"
```

---

## 四、测试

### 健康检查

```bash
curl http://localhost:8000/healthz
# {"ok":true}

curl http://localhost:8000/readyz
# {"ok":true}
```

### 提问测试（非流式）

```bash
curl -X POST http://localhost:8000/v1/qa \
  -H "Content-Type: application/json" \
  -d '{"question": "怎么退款？", "top_k": 5}'
```

响应示例：

```json
{
	"answer": "您好，退款流程如下：...",
	"citations": [{ "source": "data/shangwu11to03.xlsx", "score": 0.85 }],
	"trace_id": "xxx"
}
```

### 提问测试（流式 SSE）

```bash
curl -X POST http://localhost:8000/v1/qa/stream \
  -H "Content-Type: application/json" \
  -d '{"question": "怎么退款？", "top_k": 5}'
```

### 外部访问

API 地址：`http://47.110.33.91:8000`

```bash
# 从任何机器测试
curl -X POST http://47.110.33.91:8000/v1/qa \
  -H "Content-Type: application/json" \
  -d '{"question": "订单怎么查？"}'
```

> 注意：确保阿里云安全组已放行 8000 端口。

---

## 五、运维

### 查看日志

```bash
tail -f /root/ragchatbot/api.log       # API 日志
tail -f /root/ragchatbot/ingest_full.log  # 最近一次 ingest 日志
```

### 重启 API

```bash
pkill -f 'uvicorn api.app'
# 然后重新执行"启动 API 服务"命令
```

### GPU 监控

```bash
nvidia-smi   # 查看显存占用
watch -n 1 nvidia-smi  # 实时监控
```

### 增量添加新数据

如果有新的 xlsx 对话数据，不需要重建整个库：

```bash
source /root/miniconda3/bin/activate rag && cd /root/ragchatbot
export HF_ENDPOINT=https://hf-mirror.com PYTHONPATH=/root/ragchatbot/src PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

# 不加 --recreate，追加到已有 collection
python reingest_qiyu.py \
  --xlsx data/新文件.xlsx \
  --mode both \
  --embed-batch-size 8 \
  --upsert-batch-size 64 \
  --min-rounds 2
```

---

## 六、当前数据统计

| 数据源             | 会话数     | Chunk 数   | 跳过数        |
| ------------------ | ---------- | ---------- | ------------- |
| shangwu11to03.xlsx | 21,617     | 43,259     | 9,778 (< 2轮) |
| 知识库.md          | -          | 10         | -             |
| **总计**           | **21,617** | **43,269** | -             |

向量数据库大小：**724 MB** (`milvus.db`)

cat > /etc/systemd/system/ragchatbot.service << 'EOF'
[Unit]
Description=RAG Chatbot API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/ragchatbot
Environment="HF_ENDPOINT=https://hf-mirror.com"
Environment="PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True"
Environment="PYTHONPATH=/root/ragchatbot/src"
Environment="PATH=/root/miniconda3/envs/rag/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin"
ExecStart=/root/miniconda3/envs/rag/bin/uvicorn api.app:app --host 0.0.0.0 --port 80
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 先停掉当前 nohup 进程，交给 systemd 管理

kill $(pgrep uvicorn) 2>/dev/null; sleep 2
systemctl daemon-reload
systemctl enable ragchatbot
systemctl start ragchatbot
sleep 3 && systemctl status ragchatbot --no-pager

建议你现在做一件事 — 配置 systemd 开机自启，防止服务器重启后 API 丢失。粘贴这个：

cat > /etc/systemd/system/ragchatbot.service << 'EOF'
[Unit]
Description=RAG Chatbot API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/ragchatbot
Environment="HF_ENDPOINT=https://hf-mirror.com"
Environment="PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True"
Environment="PYTHONPATH=/root/ragchatbot/src"
Environment="PATH=/root/miniconda3/envs/rag/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin"
ExecStart=/root/miniconda3/envs/rag/bin/uvicorn api.app:app --host 0.0.0.0 --port 80
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 先停掉当前 nohup 进程，交给 systemd 管理

kill $(pgrep uvicorn) 2>/dev/null; sleep 2
systemctl daemon-reload
systemctl enable ragchatbot
systemctl start ragchatbot
sleep 3 && systemctl status ragchatbot --no-pager




之后管理 API 就用：

systemctl restart ragchatbot # 重启
systemctl stop ragchatbot # 停止
journalctl -u ragchatbot -f # 查看日志
