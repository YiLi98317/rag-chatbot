# AGENTS.md — 面向所有 AI Agent 的项目规则

> 本文件是所有 AI 编码 agent（Cursor / Claude / Copilot 等）在本仓库工作的**最高约束**。
> 与任何单个工具的默认行为冲突时，以本文件为准。

## 0. 这是什么仓库

- 这是一个**遗留（legacy）RAG 聊天机器人**仓库，正在被改造成「手机租赁公司商家问答」的后端端口。
- 代码由多人（含用 AI 快速生成的同事）分阶段拼接而成，**质量不均、存在重复与死代码**。
- 已有一个测试环境在公网 ECS 上运行（见 `docs/03-runbook.md`），**任何改动都可能影响线上**。

## 1. 硬性禁止（未经用户明确同意，一律不做）

在本阶段（仓库审计与上下文改装期），**禁止**：

- ❌ 修改业务代码逻辑（`src/`、`api/`、`eval/`、`scripts/`、根目录 `reingest_*.py`）
- ❌ 重构、重命名、移动文件或目录
- ❌ 升级 / 降级 / 新增 / 删除依赖（`requirements.txt`、`pyproject.toml`、`.venv`）
- ❌ 删除任何文件（包括看起来废弃的 `masanduo_624/`）
- ❌ 修改数据库结构、`milvus.db`、知识库数据文件
- ❌ 修改生产配置、环境变量、部署文件（`.env`、`Dockerfile`、`docker-compose.yml`、`k8s/`、`deploy_masanduo.sh`、`.github/workflows/`）
- ❌ 执行任何会连接、修改或部署到线上 ECS（`47.110.33.91`）的命令
- ❌ 打印、提交或以任何形式泄露密钥（`.env` 中的 `API_KEY` 等）

允许：阅读代码、创建/更新 `docs/`、`tasks/`、`AGENTS.md`、`CLAUDE.md`、`.cursor/` 下的说明性文件。

## 2. 工作方式：先理解 → 再计划 → 小步改

1. **先理解**：动手前先读 `docs/01-current-state.md`、`docs/02-architecture.md`、`docs/04-known-problems.md`。
2. **再计划**：改代码前，用一段话说明「要改什么、为什么、影响哪些文件、如何验证、如何回滚」，等用户确认。
3. **小步改**：一次只做一件事。每个改动都要能追溯到用户的明确需求。
4. **不美化**：不顺手「优化」相邻代码、注释、格式。不修没坏的东西。
5. **诚实标注**：确定的事写成结论，推测的事必须标「推测/待确认」。不要不懂装懂。

## 3. 变更纪律

- 每次改动前后，若涉及可运行逻辑，说明验证方式（本地能否跑、跑什么命令看结果）。
- 你的改动产生的孤儿 import/变量/函数，由你清理；**预先存在的死代码不要删**，只在文档里记录。
- 涉及密钥、部署、数据库、线上的操作，一律先停下来问用户。

## 4. 每次 session 开始必读

- `AGENTS.md`（本文件）
- `docs/99-session-log.md`（上一轮做了什么、下一步是什么）
- 与当前任务相关的 `tasks/TASK-*.md`

## 5. 关键背景（避免踩坑）

- `src/chatbot/masanduo/` 是**当前线上生效**的马三多工作流（`/v1/qa` 走它）。
- `masanduo_624/` 是同事的**独立原始版本**，与 `src/chatbot/masanduo/` 功能重叠但**不是同一份代码**，改一处不会同步另一处。哪份是权威版本**待用户确认**。
- 会话状态目前是**纯内存**（`src/chatbot/masanduo/session.py`），重启即丢，无法多实例，也没有持久化问答日志——这与用户「做后台维护+评分」的目标直接相关。
