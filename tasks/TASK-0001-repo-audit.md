# TASK-0001 · 仓库审计与下一步计划

状态：审计已完成（第一轮）。以下是**建议的后续任务顺序**，按优先级从小步到大改。
每个任务都遵守 `AGENTS.md`：先计划、等确认、再小步改。

---

## 阶段 0 · 先回答关键问题（阻塞后续，无需写代码）

- [ ] Q1 权威版本：`src/chatbot/masanduo/` vs `masanduo_624/`，用哪个？另一个能否冻结？
- [ ] Q2 线上真实部署方式：systemd + scp，还是 docker/k8s？
- [ ] Q3 `.env` 当前实际模式（DeepSeek / 本地 Ollama）？
- [ ] Q4 泄露的 DeepSeek key 是否已轮换？（安全优先，用户侧执行）
- [ ] Q5 「评分后台」形态：人工评分 / LLM 自动评分 / 结合？谁来用？
- [ ] Q6 前端 AiChat 在哪个仓库？是否已传 `session_id`？

## 阶段 1 · 让项目本地可复现地跑起来（验证优先，尽量不改代码）

- [ ] 用 3.10+ 解释器创建/确认 `.venv`，`make install` 成功（验证：无报错）。
- [ ] 确认 `.env` 可用配置（不提交、不改线上）。
- [ ] `make api` 起服务，`/healthz`、`/readyz` 返回 200（验证：curl）。
- [ ] `curl /v1/qa` 用几条真实业务问句冒烟（回收价/租机/红线/办单）。
- [ ] 若跑不起来 → 在 `docs/04-known-problems.md` 补充实际报错（只记录，不擅自修）。
- 验证标准：本地能起、能答、日志正常。

## 阶段 2 · 修「启动/构建」阻断性问题（小范围、需确认）

- [ ] 针对阶段 1 暴露的具体报错，逐个提计划 → 确认 → 最小修复。
- [ ] 统一/澄清 `.env` 文档矛盾（README vs 本地启动指南）——**优先改文档，不改代码**。
- 验证标准：全新环境按 `docs/03-runbook.md` 可一次跑通。

## 阶段 3 · 补齐会话与问答日志落库（用户核心目标地基）

> 这是「后台维护+评分」的前置。倾向复用已有 SQLAlchemy 依赖，先只做「写」。

- [ ] 设计问答日志表：session_id / role / 问题 / 检索命中(sources) / 回答 / intent-path / 耗时 / token / 是否转人工 / 时间戳。
- [ ] 在**单出口**（`masanduo/engine.respond` 已集中记日志）旁挂持久化，不改路由逻辑。
- [ ] 保留内存 session 不动，仅额外落库。
- 验证标准：每次 `/v1/qa` 都有一条完整记录入库，可查询。

## 阶段 4 · 评测 / 评分后台雏形（行业标准做法）

- [ ] 先建业务 golden set（`eval/golden.jsonl`，从真实问句 + 标准答案 + 必命中知识点 + 禁用表达 + 是否转人工）。
- [ ] 人工评分：一个最简后台/脚本，按 5 分制给历史问答打分（先能用即可）。
- [ ] 硬指标：知识库命中率 / 正确率 / 违规话术率 / 转人工识别率。
- [ ] （后）LLM 自动裁判 + 回归跑分（RAGAS / 自建）。
- 验证标准：能对一批问答产出可复现的分数报告。

## 阶段 5 · 小范围重构（仅在有测试/评测护栏后）

- [ ] 收敛 `/v1/qa` 与 `/v1/qa/stream` 逻辑分叉（P3）。
- [ ] 收敛/冻结 `masanduo_624/` 与教程数据脚本（P4，确认为死代码后）。
- 验证标准：重构前后评测分数不下降。

## 阶段 6 · 最后才考虑依赖升级 / 大改 / 模型微调

- [ ] 依赖升级（torch/transformers/pymilvus 等），逐个验证。
- [ ] 待 RAG + 评测稳定、积累高质量问答后，再评估 SFT/LoRA（用户 GPT 建议路线）。

---

### 备注
- 安全项（key 轮换、关 80 直连、去硬编码 IP）应**尽早并行处理**，但属运维/用户决策，不阻塞代码任务。
- 全程遵守：不删文件、不擅自升依赖、不碰生产，每步先说明计划。
